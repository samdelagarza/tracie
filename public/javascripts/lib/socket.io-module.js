define(['./socket.io'],function() {
    // we do this so that with the next release of
    // socket.io we don't have to modify the actual
    // file, rather it's imported here and executed
    // within the context of a module.
});